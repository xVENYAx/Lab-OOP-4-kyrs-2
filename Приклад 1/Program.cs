using System;

namespace Приклад_1
{
    class Program
    {
        static void Main(string[] args)
        {
            //Пример 1
            // Сохраняем наибольшее значение из двух, a и b,
            // в переменной max
            max = b;
            if (a > b)
            {
                max = a;
            }

            //Пример 2
            max = b;
            if (a > b) { max = a; }

            //Пример 3
            max = b;
            if (a > b) max = a;
        }
    }
}
